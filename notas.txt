Docker network créate richi
Docker network ls
Docker volume créate vol_richi
Docker volume ls

docker run -d -p 3306:3306 --name sqlrichi --network richi -e MYSQL_ROOT_PASSWORD=4477 MySQL

docker run -d -p 3307:3306 -v my_data:/var/lib/mysql -e MYSQL_ROOT_PASSWORD=12345 MySQL

docker run -d -p 8080:80 --name phpmyadmin_richi --network richi -e PMA_HOST=sqlrichi -e PMA_password=4769 phpmyadmin